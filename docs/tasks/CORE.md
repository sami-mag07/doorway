# CORE
Owns: Next.js scaffold, `lib/`, `app/api/places`, `data/`, Docker/Caddy.
1. By 15:00: Next.js (App Router, TS) scaffold, empty routes for all five screens, i18n DE + EN.
2. By 15:00: fixtures `data/places/ai-center.json` and `data/routes/ai-center-toilet/route.json` matching the contracts in README.md.
3. By 15:30: deployed on the VPS behind Caddy with HTTPS (sslip.io), reachable from the iPhone.
4. Places API route: read and write place JSON, validate input.
