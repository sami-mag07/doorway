# MAP
Owns: `/`, `/business`, map components.
1. User view: profile choice, search, `Map3DElement` with entrance markers (shape + color per type), profile filter, "Take me to the toilet".
2. Business onboarding: accessibility checkbox, draggable entrance markers, entrance detail form prefilled by AI, confirm button, visibility score.
3. Fallback: static map image if the Maps key is missing.
