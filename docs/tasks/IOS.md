# IOS
Owns: `ios/`.
1. SwiftUI + ARKit/RealityKit app that loads `route.json` from the server URL.
2. Detect the floor plane, place an arrow for the current waypoint, show the instruction.
3. Advance by tap first, then by heading change.
